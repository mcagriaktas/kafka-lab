#!/bin/bash

echo "Setting up git configuration"
# Configure git
git config --global user.name "your_name"
git config --global user.email "your_email"

echo "Starting machine-starter.sh"
tail -f /dev/null